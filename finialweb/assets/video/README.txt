Drop the hero background video here as: hero.mp4

Used by: js/components.js (renderHero) -> <video><source src="assets/video/hero.mp4"></video>
Recommended: muted, looping, ~10-20s, 1920x1080, H.264 mp4, under ~8MB for fast load.
Until this file exists, the poster image (first hero slide image) shows instead.
