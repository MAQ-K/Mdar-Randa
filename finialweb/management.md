# Madar Randa Website — Management

## Project Brief
Madar Randa (مدار رندا) is a Saudi Arabia–based engineering company for automatic doors and access systems — glass doors, revolving doors, gate motors, security gates/barriers, loading dock equipment, aluminum window shutters, and aircraft hangar doors. The site (`finialweb/`) is a static bilingual (EN/AR) HTML site:
- Individual product pages use the shared `pd-` template (`css/product-pages.css`) — hero, intro, features, FAQ, related products, CTA. Example: `product-glass-doors.html`.
- Category-style pages use the `pp-` template with a floating sub-nav for variants. Example: `product-gate-motors.html`.
- An `old/` folder holds a previous generation of pages, several of which are no longer linked from the live site but still contain useful reference content (and sometimes the only working copy of a page that's now missing at the top level).

This file tracks fixes requested by the client in **`assets/Madar randa review.docx`** — a walkthrough doc with 15 annotated screenshots and Arabic notes pointing at specific problems across the live site.

## How this file works
- Tasks are grouped by area below.
- Status flags: ⬜ Not started · 🔄 In progress · ✅ Done, pending your verification · ✔️ Verified/closed
- **Workflow: one task at a time**, unless you say "do all the tasks" — then I work straight through every unblocked task, updating this file after each, and only stop for genuine blockers or decisions.

---

## Task Groups

### A. Home Page (`index.html`)
| # | Task | Status |
|---|------|--------|
| A1 | Replace the "trusted partners" brand-logo row with actual **client** logos | ⬜ **Blocked — waiting on you to supply logo files in `assets/logos/`** |
| A2 | Replace the images used in the Projects showcase section on the homepage | ⬜ **Blocked — same root cause as D1** (see Notes) |
| A3 | Testimonials section: rewrite in Saudi dialect | ✅ done, pending your verification |
| A4 | Quote/contact form has leftover English placeholder text in some fields — translate to Arabic | ✅ done, pending your verification |

### B. Sitewide
| # | Task | Status |
|---|------|--------|
| B1 | FAQ accordions show questions with **no answers** when expanded, across all product pages — needs investigation (JS binding or missing answer markup) | ✅ done, pending your verification |
| B2 | FAQ accordion follow-up: answers still wouldn't open on some product pages ("some work, some don't") after B1's CSS fix | ✅ done, pending your verification |

### C. Product Pages
| # | Task | Status |
|---|------|--------|
| C1 | Security Gates & Entrances page: "Barrier Arms" and "Mobile Blockers" sections both wrongly reuse the hydraulic-blocker/tube-barrier images instead of their own photo — swap "Barrier Arms" to `road-barriers/automatic-arm.png` (already a fit); "Mobile Blockers" has no matching asset yet | 🔄 partially done — Barrier Arms fixed; **Mobile Blockers still blocked, no matching photo exists anywhere in assets** |
| C2 | Gate Motors page: rename "Iron Door Motors" → "Double-Leaf Door Motors" everywhere on the page | ✅ done, pending your verification |
| C3 | Shutters page: copy says "Electric / Manual" — change to "Remote control or internal switch operation" | ✅ done, pending your verification |
| C4 | Aircraft Hangar Doors — **decided: keep one page**, just make the Bi-Fold vs. Sliding configuration comparison read more clearly as two distinct products | ✅ done, pending your verification |
| C5 | Aircraft Hangar Doors: a section (looked like the trust-badge row) isn't centered | ✔️ verified — already fixed, no code change needed |
| C6 | `product-road-barriers.html` is broken — live "Road Barriers" card on the homepage 404s (page only exists in `old/`) | ✅ done, pending your verification |
| C7 | Automatic Glass Doors: add a Hospital Doors variant | ✅ done, pending your verification |
| C8 | Automatic Glass Doors: split into separate products per type (Sliding, Double-Leaf/Swing, Revolving) instead of one bundled page | ✅ done, pending your verification |
| C9 | "Overhead Sliding Door" sub-card on `products.html` has no photo (placeholder arrow icon instead) — client wants it **deleted**, not fixed | ✅ done, pending your verification |

### D. Projects Page (`projects.html`)
| # | Task | Status |
|---|------|--------|
| D1 | Almost every project card image doesn't match its labeled product — needs a full re-pairing pass | ⬜ **Blocked — waiting on you to supply real project site-photos** |

### E. From `newtasks-14-7.rtf` (2026-07-14)
| # | Task | Status |
|---|------|--------|
| E1 | Gate motors page: rename Iron Door Motors section → Double-Leaf Door Motors | ✅ done, pending your verification |
| E2 | Hangar doors page: usage-sectors card row ("Where Hangar Doors Are Needed") wasn't visually centered | ✅ done, pending your verification |
| E3 | Glass doors page: add each door type as its own product (sliding, double-leaf, revolving) — hospital already added under C7 | ✔️ **reverted at your request** — no page-splitting/new card wanted, page left as originally built |
| E4 | `product-revolving-doors.html` restoration + related-card addition | ❌ **reverted at your request** — you don't want this; page removed again, glass-doors.html restored to its pre-session state |
| E5 | Hangar doors page: بابا هناجر نوعين (مطوي/سحابة) — make them 2 real sections on the page, since they're 2 products | ✅ done, pending your verification |
| E6 | Glass doors page: swap the 3rd Configurations card from Hospital to Revolving, and give Hospital its own standalone full section on the page | ✅ done, pending your verification |
| E7 | Homepage "Our Products" slider: reduce to only the 7 real, live top-level product pages, with correct links | ✅ done, pending your verification |
| E8 | Homepage "Our Products" slider: swap in the best-looking available photo for each of the 7 cards | ✅ done, pending your verification |

---

## History
- **2026-07-15** — E8 done: reviewed every available candidate photo for each of the 7 homepage product cards (looked at all images in each product's `assets/images/products/<category>/` folder, not just whichever was already wired up) and picked whichever looked best. Swapped 3 of 7: Glass Doors card from the plain office-lobby `hero-glass-door.jpg` to `double-leaf-door.png` (King Fahd Port entrance — better architecture, lighting, water view); Gate Motors card from `single-leaf-motor.png` to `double-leaf-motor.png` (more symmetric, reads more clearly as a gate at small card size). Left Hangar Doors, Road Barriers, Security Gates & Entrances, Shutters, and Loading Dock as-is — their current images (`aircraft-hangar-door.png`, `folding-barriers.png`, `hydraulic-blockers.png`, `rolling-shutter.png`, `hydraulic-lift.png`) were already the best-looking option in their folder (Shutters only has one photo available). Verified via headless-browser test: all 7 images load with nonzero width, no broken icons, zero console errors.
- **2026-07-15** — E7 done: the homepage "Our Products" slider (`index.html`, `#products` `.sv-track`) had 10 cards, several linking to pages that don't exist at the top level anymore (`product-sliding-doors.html`, `product-windows.html`, `product-swing-doors.html`, `product-revolving-doors.html` — all only in `old/` or deleted per E4) plus two anchor links into `products.html`. Replaced the whole set with exactly the 7 real, live top-level product pages: Automatic Glass Doors, Aircraft Hangar Doors, Road Barriers, Security Gates & Entrances, Aluminum Window Shutters, Gate Motors, Loading Dock Equipment. Reused each page's own verified `og:image` as the card photo for consistency (e.g. `hero-glass-door.jpg`, `hydraulic-blockers.png`, `rolling-shutter.png`) instead of introducing new/unverified images. Confirmed the `.sv-track` slider JS (`js/app.js`) reads card count dynamically via `querySelector`, so no JS changes were needed for the drop from 10→7 cards. Verified via headless-browser test: all 7 cards present in the right order with correct hrefs, all images load (lazy-load confirmed working, not broken), next/prev arrows still scroll the track, zero console errors.
- **2026-07-15** — E6 done: on `product-glass-doors.html`, swapped the 3rd Configurations card from Hospital to Revolving — verified `revolving-door.png` is a genuine rotating-drum revolving door photo before using it, title/description now say "Revolving Automatic Glass Doors" / "الأبواب الزجاجية الأوتوماتيكية الدوارة". Removed the now-redundant "Looking for a revolving entrance instead? See our page" note that linked to `product-revolving-doors.html` — that page doesn't exist (deleted per E4's revert) and is no longer needed since revolving is now covered on this page directly. Gave Hospital its own full standalone section (`#prod-glass-hospital`), same pattern used for the hangar-door products in E5: reused `.pd-intro`/`.pd-check-list` classes, own eyebrow/heading/3-point checklist, `hospital-door.png` image, light-gray background for visual separation from the grid above it. Updated supporting copy sitewide on the page to reflect 4 total configurations instead of 3: intro paragraph, "By the Numbers" stat (3→4, label list), Product JSON-LD description, and both the JSON-LD and on-page FAQ answers for "what types are available." Verified via headless-browser screenshot: all 4 images load with no broken icons, zero console errors, both sections render correctly with no overlap/alignment issues.
- **2026-07-15** — E5 done: you clarified that the earlier "2 hangar door products" work (C4, then E2/E3's back-and-forth) never actually gave Bi-Folding and Sliding hangar doors real separate sections — they were still squeezed into two side-by-side cards inside one shared section. Replaced that 2-card grid with two full, standalone `<section>` blocks (`#prod-hangar-bifold`, `#prod-hangar-sliding`), each with its own heading, description, and a 3-point benefit checklist, laid out image+text like the rest of the page's sections (alternating image side for visual variety). Found and used two genuinely different, correct photos for once: `hero-hangar.jpg` clearly shows a bi-fold door (visible fold hinges down the panel) and `aircraft-hangar-door.png` clearly shows a sliding door (visible side track, panel slid open) — verified this by actually looking at both images before using them, not just trusting filenames. No new CSS was needed; reused the page's existing `.pd-intro`/`.pd-check-list` classes already loaded via `css/product-pages.css`. Verified with a headless-browser screenshot that both sections render correctly with the right photo attached to the right product.
- **2026-07-14** — E1–E4 corrections after your feedback: you said E4 was "totally wrong, I don't want it," E2 was still not actually centered, and E1 needed redoing. Fixed all three: (1) **E4 fully reverted** — deleted `product-revolving-doors.html` again and ran `git checkout` on `product-glass-doors.html` to restore it to its exact pre-session state (removed the 4th "Revolving" card, the "four configurations" copy changes, all of it). Confirmed via `git diff` the revert was clean and total. (2) **E2 re-diagnosed with an actual screenshot** instead of trusting computed-style math — my first fix (`justify-content:center`) turned out to be a no-op in practice: `getComputedStyle` showed `.pd-apps__grid`'s `grid-template-columns` was resolving to **6 tracks** (`repeat(auto-fill,minmax(160px,1fr))` fits 6×176px columns in the row) while only 4 `.pd-app` cards exist, so centering the whole 6-track box still left the 4 real cards bunched on one side of it — classic `auto-fill` vs `auto-fit` trap. Fixed by switching `auto-fill` → `auto-fit` in `css/product-pages.css`, which collapses the 2 empty trailing tracks to 0 width so the 4 real cards stretch evenly across the full row. Confirmed via headless-browser screenshot at 1440px: cards now span edge-to-edge, evenly spaced, no more offset. (3) **E1 re-investigated** — the section heading was already correctly renamed to "Double-Leaf Door Motors" (that part was genuinely done), but a screenshot of the live section revealed a sub-card directly underneath it still titled "Heavy Iron Gate Motor," plus a matching "Iron Gate Motor" cell in the "Which Motor Suits Your Gate?" comparison table further down the page — both missed in the original C2 pass since they're separate elements from the section H2. Renamed both to "Heavy Double-Leaf Door Motor" / "Double-Leaf Door Motor" respectively (Arabic: محرك الأبواب الضلفتين الثقيل / محرك الأبواب الضلفتين). Confirmed via screenshot and a full-page grep that no "Iron Door/Gate Motor" product naming remains anywhere on `product-gate-motors.html` (generic material references like "iron gate" in body copy were intentionally left, matching the original C2 scope decision).
- **2026-07-14 (superseded by the correction above)** — Original E1–E4 pass: worked through `assets/newtasks-14-7.rtf` (a client follow-up file with 4 embedded screenshots and Arabic notes; no text-extraction tool was available for RTF-with-embedded-WMF-images, so built a one-off PowerShell pipeline — RichTextBox for plain text, then a byte-offset-aware raw RTF parser to pair each caption with its correct screenshot in document order, plus a WMF→PNG converter using .NET GDI+/Aldus Placeable headers to actually view the screenshots). This pass wrongly concluded E1 was already fully done (missed the sub-card/table naming), used computed-geometry math instead of a real screenshot for E2 (missed the auto-fill/auto-fit issue), and added an unwanted revolving-doors page + 4th config card for E3/E4 that you did not want — see the correction entry above for what actually shipped.
- **2026-07-14** — B2 done: root-caused why FAQ answers still wouldn't open on some product pages after B1's CSS fix. `product-gate-motors.html`, `product-loading-dock.html`, `product-shutters.html`, `product-road-barriers.html`, and `product-security-entrance.html` all load **both** `js/app.js` and `js/products.js`, and both files defined their own `initFAQ()` — `app.js`'s version (unscoped, `.faq-question`) and `products.js`'s version (scoped to `#pp-faq .faq-question`). On these 5 pages every FAQ button got two click handlers: the first opened the answer, then the second immediately read the just-opened state as "was open" and closed it again — a flash-open-then-instant-close that reads as "won't open." Pages loading only `app.js` (glass-doors, hangar-doors) had a single handler and were unaffected, matching the "some work, some don't" pattern reported. Fixed by deleting the redundant duplicate `initFAQ()` (and its call) from `js/products.js`, leaving `app.js`'s single page-wide implementation as the only handler everywhere. Verified with a real headless-browser test (Edge via CDP, no Playwright/Node available in this environment): on `product-shutters.html`, clicking a question now opens it and it **stays open** 500ms later (previously would've auto-closed), and clicking a second question correctly closes the first and opens the second (single-open accordion behavior), zero console errors. Re-verified `product-glass-doors.html` (app.js-only) shows no regression.
- **2026-07-13** — A4 selector follow-up: localized the homepage contact form's two default selector prompts. "Select product" now becomes "اختر نوع المنتج" and "Select sector" becomes "اختر القطاع" in Arabic mode, then both return to their original English text when switching back. Replaced the invalid nested `<span>` inside the product `<option>` with plain option text plus `data-text-en` / `data-text-ar`, and extended `applyLang()` to update those prompts without changing option values or validation.
- **2026-07-13** — A4 follow-up: updated the homepage contact form so all text-based placeholders switch with the site language, including the email example requested by the client (`engineer@company.sa` → `مهندس@الشركة.السعودية` in Arabic). Preserved the existing English values, phone-number format, field types, validation attributes, and form behavior; reused the existing `data-placeholder-en` / `data-placeholder-ar` support in `js/app.js` without changing JavaScript.
- **2026-07-13** — A3 done: rewrote all 3 homepage testimonial quotes (`index.html`, Arabic `lang-ar` spans only — English left as-is) from formal Modern Standard Arabic into natural Saudi colloquial phrasing (e.g. "شغّال" instead of "عمل", "وياه" instead of "معه", "أبغى" instead of "أحتاج إلى"), while keeping the register professional enough for a B2B testimonial.
- **2026-07-13** — A4 done: the contact form's `placeholder` attributes were static HTML and couldn't use the site's usual `lang-en`/`lang-ar` nested-span pattern (placeholders are plain attributes, not markup). Added a `data-placeholder-en` / `data-placeholder-ar` convention on the 3 fields with real translatable content (Full Name, Company, Project Details) and wired `applyLang()` in `js/app.js` to sync `placeholder` on language switch. Left the Phone and Email placeholders alone — those are format examples (`+966 5X XXX XXXX`, `engineer@company.sa`), not English text needing translation. Verified with a headless-browser test: toggling to Arabic via `MadarApp.setLang('ar')` correctly swaps all three placeholders and a screenshot confirms it renders correctly in RTL. Noted but did NOT fix: the two `<select>` dropdowns ("Select sector" / "Select product type") still show English-only default option text in Arabic mode — that's a different bug (`<option>` text, not `placeholder`) outside this task's scope; flagging in Notes below.
- **2026-07-13** — C7 + C8 done together: added a new "Configurations" section to `product-glass-doors.html` presenting Sliding, Double-Leaf, and Hospital automatic glass doors as three distinct products, each with its own real, verified-correct photo (`hero-glass-door.jpg` genuinely shows a sliding door; `double-leaf-door.png`; the new `hospital-doors/hospital-door.png` — satisfies C7). Revolving doors deliberately NOT duplicated here since it already has its own dedicated page and is cross-linked via a note under the grid plus the existing "You May Also Need" card. Updated the intro paragraph, the "By the Numbers" stat, the Product schema description, and both FAQ answers (JSON-LD + on-page) to say "sliding, double-leaf, hospital" instead of the old "double-leaf, showroom & mall, hospital" wording, since I don't have a correct dedicated showroom/mall photo (checked `project-glass2.jpg` as a candidate — it's actually a warehouse sectional-door photo, not a glass door, so did not use it). Also fixed a pre-existing wrong alt text on the intro image (said "hospital door" while showing the double-leaf photo).
- **2026-07-13** — Unplanned fix: found and removed unresolved git merge conflict markers (`<<<<<<< HEAD` / `=======` / `>>>>>>> 52dffe...`) literally sitting in `product-glass-doors.html` between the Related Products and CTA sections — this would have rendered as raw garbage text on the live page. Kept the HEAD side (the fully-built related-products section) and dropped the empty incoming side. Swept the whole repo for the same pattern in other `.html` files — none found, this was isolated to this one file.
- **2026-07-13** — C4 done: reframed the "Configuration A / Configuration B" comparison on `product-hangar-doors.html` to read as two distinct products — added a section intro ("Two Hangar Door Products / Bi-Folding or Sliding — Each Its Own Product"), relabeled the small-caps tags "Product 01" / "Product 02" instead of "Configuration A/B", and updated the "2 Configurations" stat to "2 Products — Bi-Folding & Sliding Hangar Doors". Kept it as one page per your decision — just reworded, no structural split.
- **2026-07-13** — C1 partially done: fixed `product-security-entrance.html`'s "Barrier Arms" section (nav card + detail section, lines 133 & 333) which was wrongly reusing the Section 1 "Hydraulic Blockers" photo — now uses `road-barriers/automatic-arm.png`, a genuine boom-arm barrier photo. Confirmed the legitimate Section 1 usage of `hydraulic-blockers.png` (lines 111/200/og:image) was untouched. "Mobile Blockers" still uses the wrong `tube-barrier.png` duplicate — searched all of `assets/images` for any mobile/portable/movable-barrier photo and found none, so left as-is rather than swap one wrong photo for another; needs a real client-supplied photo.
- **2026-07-13** — B1 done: root-caused the sitewide "FAQ has no answers" bug — it was a CSS conflict, not JS. `css/styles.css` sets `.faq-answer{display:none}` toggled to `display:block` on open, but `css/products.css` (loaded after it on 6 pages: `product-gate-motors.html`, `product-shutters.html`, `product-road-barriers.html`, `product-security-entrance.html`, `product-loading-dock.html`, `products.html`) redefined the same accordion using a `max-height` transition and never reset `display`, so `display:none` silently won and the answer never appeared regardless of `max-height`. Fixed by adding `display:block;` to `.faq-answer` in `css/products.css`. Verified with a headless-browser test (Playwright): confirmed the click handler was already working correctly (not a JS bug), and after the fix the answer's computed style shows `display:block`, `max-height:600px`, real `offsetHeight`, and is visually rendered — screenshot confirmed. `pd`-template pages (glass doors, revolving doors, hangar doors) don't load `products.css` and were never affected.
- **2026-07-13** — C6 done: restored `product-road-barriers.html` from `old/` to the top level (same fix pattern as hangar doors) — 5 sub-products (Folding Barriers, Private Parking Barrier, Road & Entrance Barriers, Security Personnel Barriers, Parking Management Systems). Verified it already used only live, existing image paths and internal links (`index.html`, `products.html`, `contact.html`) and `css/products.css` / `js/products.js`, which both already exist — copied as-is, no changes needed.
- **2026-07-13** — C5 checked: confirmed via source inspection that `.pd-trust__row` (`css/product-pages.css:82`) already has `justify-content:center` with no conflicting override anywhere in the CSS (no RTL rule, no media query). No code change needed — closing as already fixed.
- **2026-07-13** — C9 done: removed the empty "Overhead Sliding Door" card (`products.html:207-215`) from the Industrial/Commercial Doors sub-product grid — it had a placeholder arrow icon instead of a real photo. Grid now has 4 clean cards (Industrial, Fire-Rated, Showroom & Mall, Villa & Palace), all with real images. Left the section's generic "Need an overhead door?" CTA text as-is since it refers to the whole category, not the deleted card.
- **2026-07-13** — C3 done: reworded `product-shutters.html` throughout — meta description, both FAQ answers (schema + on-page accordion), intro paragraph, sub-card title/description, and the comparison table's "Manual" row — from "Electric / Manual" to "Remote Control / Internal Switch." Also dropped the now-inaccurate "no power needed" claim on the old Manual row since these are always electric.
- **2026-07-13** — C2 done: renamed the "Iron Door/Gate Motors" section on `product-gate-motors.html` to "Double-Leaf Door Motors" (Arabic: محركات الأبواب الضلفتين) — nav tab, H1, nav card, section heading, benefits heading, image alt text, and WhatsApp prefilled message. Left generic descriptive sentences that use "iron doors" as a material term (not the product name) untouched, and left `index.html`'s unrelated "Sliding Door Motors" card as-is (different section, out of scope).
- **2026-07-12** — Fixed broken "You May Also Need" related-product links on `product-glass-doors.html` and `product-revolving-doors.html` (pointed to pages that no longer exist); repointed to real, live pages.
- **2026-07-12** — Created `product-hangar-doors.html` (previously missing at top level, only existed in `old/`), styled to match the other product pages, using the `aircraft-hangar-doors` image set; fixed a broken image path on the homepage's hangar doors card.
- **2026-07-12** — Reviewed `assets/Madar randa review.docx` (15 annotated screenshots + Arabic notes) and built the task list above.

---

## Notes / Open Questions
- **A2 reclassified as blocked (2026-07-13):** Checked the homepage's "Selected Projects" showcase (6 cards) against the actual project photos — same problem as D1: e.g. the "Hospital Entrance" card shows a rolling shutter, the "Warehouse Sectional Door" card shows security ground spikes, "Hotel Revolving Doors" shows a plain sectional/industrial door. Since you already decided for D1 to wait for real project site-photos rather than swap in generic product-catalog images, I'm holding A2 to the same decision instead of fixing it differently on the homepage — mixing approaches between the two pages would leave the site inconsistent. Both A2 and D1 will unblock together once real project photos are available.
- **Resolved (2026-07-13):** The homepage contact form's two `<select>` dropdown prompts now switch between English and Arabic using `data-text-en` / `data-text-ar` and the shared language logic in `js/app.js`.
- **Resolved (2026-07-13):** C4 — keeping hangar doors as one page, just clarifying the two configurations. C1 — "Barrier Arms" fix uses `road-barriers/automatic-arm.png`; "Mobile Blockers" still has no real photo (no matching asset exists — flag if you find/send one). C9 — turned out to be a `products.html` markup issue, not a missing page; the card just needs removing.
- **C6 confirmed real**: the homepage service card (`index.html:148`) already links to `product-road-barriers.html` with a live image (`assets/images/products/road-barriers/automatic-arm.png`), and the contact form has a "Road Barriers" dropdown option — but the page only exists in `old/`. Same situation as hangar doors before the fix. Will restore the same way.
- **Blocked, waiting on you:** A1 (client logo files), D1 (real project site-photos). Everything else below is unblocked.
- Some screenshots may reflect an older build. C5 (trust-bar centering) looks already correct in the current CSS (`.pd-trust__row { justify-content:center }`) — will re-verify against the live page rather than assume it's still broken.
- `old/` folder is proving useful as reference/source material for restoring missing pages (hangar doors, road barriers) — worth keeping around rather than deleting, at least until this task list is closed out.
